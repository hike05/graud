# Domain configuration
DOMAIN=proxy.example.com

# Let's Encrypt contact email
EMAIL=admin@example.com

# Naive proxy credentials
NAIVE_USER=username
NAIVE_PASS=secure_password

# CrowdSec bouncer API key (auto-generated by install.sh)
# Manual generation: docker exec crowdsec cscli bouncers add graud-bouncer -o raw
BOUNCER_KEY=
